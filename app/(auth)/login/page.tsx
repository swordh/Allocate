import LoginForm from '@/components/auth/LoginForm'

// Server Component shell — LoginForm handles all interactive Firebase Auth logic.
export default function LoginPage() {
  return (
    <main>
      <LoginForm />
    </main>
  )
}
